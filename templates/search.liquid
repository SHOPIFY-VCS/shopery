{% if search.performed %}
  {% for result in search.results %}
    {% if result.object_type == 'page' %}
      <h1>page</h1>
    {% else %}
      <h1>article</h1>
    {% else %}
      <h1>product</h1>
    {% endif %}
  {% endfor %}
{% else %}
  <h1>Search not Working</h1>
{% endif %}
